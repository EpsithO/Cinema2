<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détail du film</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <h1>Détail du film</h1>

    <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{ film.titre }}</h5>
            <p class="card-text"><strong>Durée :</strong> {{ film.duree }} minutes</p>
            <p class="card-text"><strong>Séances :</strong></p>
            <ul>
                {% for seance in seances %}
                    <li>{{ seance.dateProjection | date('d/m/Y H:i') }}</li>
                    <button class="btn btn-secondary seance-btn">Réserver</button>
                {% endfor %}
            </ul>
        </div>
    </div>

    <a href="{{ path('film_list') }}" class="btn btn-primary mt-3">Retour à la liste des films</a>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>